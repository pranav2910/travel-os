# Defects from the qa-20260923-cda5ccb audit — root cause, change, evidence

Status: every defect below is fixed and verified in this run (`final-report.md` §3), except BUG-11,
which the contract review reclassified as not a defect (documented). The last column names the
`-before` rows executed against the audited images before the fix was deployed (all FAIL, as the
audit found; BUG-07's race did not recur in seven attempts on the old images — the audit's own
`p3-idempotency-concurrent.json` is its before evidence) and the `-after` rows plus the audit suites
that exercise the same path (all PASS).

| Bug | Sev | Root cause (as found in the code) | Change | Regression test (fails before, passes after) | Reproduced / verified (row ids in `results.csv`; every `-before` row FAILED on the audited images, every `-after` row PASSED on the repaired ones) |
|---|---|---|---|---|---|
| BUG-08 cancelled trip keeps its confirmed reservation | P0 | `TripService.cancel` moved BOOKED → CANCELLED in one write and published `travel.trip.cancelled`; nothing consumed that event to release the order (Travel Core and Order have no Kafka consumers; only the worker does). The state table allowed BOOKED → CANCELLED directly. | New status `CANCELLING` (BOOKED → CANCELLING → CANCELLED only). `cancel` on a booked trip publishes `travel.trip.cancellation-requested`; the worker starts `TripCancellationWorkflow` (id `cancel:<tripId>`, one per trip), which calls the Order service's cancel (now resumable: CANCELLATION_PENDING persisted first, released items skipped on retry, refusals become `CANCELLATION_REFUSED` exposures with the order left CANCELLATION_PENDING/`CANCELLATION_INCOMPLETE`, per-item refunds recorded), reports `travel.trip.cancellation-incomplete` on refusal, waits (30 s → hourly, ≤30 days) for exposure resolution, and moves the trip to CANCELLED only when the order is. Cancel while planning/awaiting approval signals the workflow (`cancelled`), a late approval is refused (`TRIP_NOT_AWAITING_APPROVAL`), a refused BOOKING transition after a cancel ends the workflow without an order and without a FAILED. Audit ledger and web app know the state. | `TripStatusTest.aBookedTripIsReleasedAtTheSuppliersBeforeItIsCancelled`; `TripLifecycleIntegrationTest.cancellingABookedTripReleasesTheReservationBeforeItIsCancelled`, `.aWithdrawnTripCanNeitherBeApprovedNorBooked`; `OrderIntegrationTest.aRefusedCancellationKeepsTheOrderPendingWithAnExposureUntilAPersonReleasesIt`, `.aLostCancellationAnswerResumesFromTheItemStatesAndReleasesNothingTwice`; `TripCancellationWorkflowTest` (7 cases: release, refusal + resolution, lost answer retried with one key, pending resume, uncancellable order, not-cancelling trip, 30-day give-up); `TripWorkflowTest.aTripWithdrawnWhileAwaitingApprovalBooksNothing`, `.aTripCancelledJustBeforeBookingBooksNothingAndDoesNotFail`; `web/e2e/cancellation.spec.ts` (2 journeys) | BUG-08-1..4 (-before/-after), P3-LIFE-01, P9-RACE-02, P1-FAIL-01c |
| BUG-01 any code books as an airport (+ BUG-10 24-airport table on the itinerary path only) | P0 | The round-trip path validated only `^[A-Z]{3}$`; the itinerary path checked the 24-entry zone table; the sandbox generates inventory for any code. | One catalog (`Locations`, ~100 airports) applied at creation on both paths (`UNKNOWN_LOCATION`), city codes explained with their airports (`NYC → JFK, EWR, LGA`), free-text intents validated the same way on extraction. | `LocationsTest`, `IntentValidationTest.anyThreeLettersIsNotAnAirport`, `.aCityCodeNamesTheAirportsItStandsFor`, `.everyLegOfAnItineraryIsCheckedAgainstTheCatalog`; `TripApiIntegrationTest.CatalogAndClock.anyThreeLettersIsNotAnAirport`, `.aCityCodeIsResolvedToItsAirportsForTheRequester`; `ItineraryApiIntegrationTest.placesAndCurrenciesOutsideTheCatalogAreRefusedWithTheReason` | BUG-01-1..5, BUG-10-1, P4-R20..R22, P4-R03, P6-S13/ML-* |
| BUG-03 past departures book | P0 | No comparison with the clock anywhere; the workflow booked whatever it was approved for. | `DEPARTURE_IN_PAST` at creation (window closed at or before the platform clock; message in the departure airport's local time; a still-open window that started earlier is accepted), `STAY_IN_PAST` for stays, the same on free text; `DEPARTURE_PASSED` (stage REVALIDATION) in both workflow flows right before BOOKING. | `IntentValidationTest` (yesterday, later today, deadline at now, Boston midnight vs UTC, return window, stays); `TripApiIntegrationTest.CatalogAndClock.departuresInThePastAreRefusedInTheDeparturesLocalTime` (pinned test clock advanced 60 days; yesterday; closed a second ago; later today); `TripWorkflowTest.aDepartureThatPassedWhileTheTripWaitedIsNotBooked` (pinned workflow clock) | BUG-03-1..6, P3-VAL-14, P4-R26 |
| BUG-07 one of five concurrent identical requests answers 500 | P1 | `findByIdempotencyKey` then INSERT inside one transaction: the loser hit the unique constraint after the lookup. | `INSERT … ON CONFLICT ON CONSTRAINT trip_idempotency DO NOTHING`; 0 rows → re-read by key and replay (same fingerprint) or `IDEMPOTENCY_KEY_REUSED`. | `TripApiIntegrationTest.CatalogAndClock.concurrentIdenticalRequestsMakeOneTripAndEveryAnswerIsTheSame` | BUG-07-1, P3-MUT-02 |
| BUG-05 unknown currency reaches the optimizer; Java exception name as failure code | P1 | `Currency.getInstance("XXX")` passes; the workflow's `failureCode()` used Temporal's failure type, which is the exception class name for unclassified errors. | `CURRENCY_UNSUPPORTED` at creation (supported: USD, what the sandbox airline quotes; the first after-phase run showed a GBP trip accepted and then failing at OPTIMIZATION with an internal error, so GBP was removed from the set; documented); `FailureCodes`: class-name types → `PLATFORM_ERROR`, message kept. | `IntentValidationTest.onlyCurrenciesTheSuppliersQuoteAreAccepted`; `ItineraryApiIntegrationTest` (XXX); `TripWorkflowTest.anUnclassifiedActivityErrorIsAPlatformErrorNotAClassName` | BUG-05-1..3, P3-ITN-03 |
| BUG-11 refund of zero accepted | P1 → not a defect | Re-examined against the contract: a settled refund of zero is a legitimate outcome (a non-refundable rate settled at nothing) and must be ledgered so the learning evidence is complete; only negative amounts are invalid. Refund status (`REFUND_SETTLED`) is distinct from cancellation status. | Contract documented (`RefundRequest` javadoc, README pilot scope); behaviour unchanged; test added. | `LearningIntegrationTest.feedbackIsAuthorizedValidatedAndRevisioned` (zero 200, negative 400) | BUG-12-1, P5-MONEY-ZERO |
| BUG-12 refund currency mismatch not flagged | P2 | The learning service did not know the order's currency. | Item currency recorded from `travel.order.confirmed/changed` (learning V2 migration); `CURRENCY_MISMATCH` (422) when the refund's currency is not the order's; the platform never converts. | `LearningIntegrationTest.feedbackIsAuthorizedValidatedAndRevisioned` (EUR on USD → 422) | BUG-12-1, P5-MONEY-WRONG |
| BUG-04 arranged trips carry an empty traveler snapshot | P1 | `TripService.create` stored blanks for an arranger's trip; the workflow then booked with an empty passenger name. | `traveler{givenName, familyName, email}` required for arranged trips (`TRAVELER_IDENTITY_REQUIRED`), stored in the snapshot, in the idempotency fingerprint; web form fields. | `TripApiIntegrationTest.Security.onlyArrangersCreateTripsForOthers`; `TripLifecycleIntegrationTest.aTripArrangedForSomeoneElseIsBookedInTheirName`, `.aServiceArrangingATripThroughGrpcNamesTheTravelerToo` (gRPC door: Enterprise Context now sends the verified HRIS identity — found by CI's kind slice-4 case after the first commit) | BUG-04-1, P1-PERSONA-01/01c, kind slice-4 concurrent conversion |
| BUG-06 negative list limit silently clamped | P2 | `Math.clamp(limit, 1, 200)`. | `LIMIT_OUT_OF_RANGE` (422) outside 1..200. | `TripApiIntegrationTest.CatalogAndClock.listLimitsOutsideTheRangeAreRefusedNotClamped` | BUG-06-1, P3-LIST-01..03 |
| BUG-09 no CSP/nosniff/frame-options on the edge | P2 | nginx `add_header` is not inherited into a `location` that sets its own header; every application location did (Cache-Control). | One header snippet included by every location and the proxy snippet: nosniff, frame-options, referrer-policy, permissions-policy, CSP (`'self'` + the identity provider's origin derived from `OIDC_AUTHORITY`). | none automatable in Java; `p11_security.py` P11-HDR-01, BUG-09-1, browser sessions `p02_session.js`, `web/e2e/auth.spec.ts` (sign-in/renew under CSP) | BUG-09-1, P11-HDR-01 |
| BUG-14 outage returns nginx HTML 502 | P2 | No `proxy_intercept_errors`/`error_page`. | `application/problem+json` 503 `UPSTREAM_UNAVAILABLE` (502/503) and 504 `UPSTREAM_TIMEOUT`, with the security headers and `Retry-After`. | `repair_regressions.py` BUG-14-1 (stops travel-core, reads the edge), `p12_outages.py` | BUG-14-1, P12-OUT-* |
| BUG-13 contrast 4.26:1 on the trip page's skipped step | P2 | `--text-3` (#6b7280) on `--surface-2` (#eef1f5). | `--text-2` (#4b5563, 7.0:1). | `p10_frontend.js`/`p10b_keyboard.js` axe scans (no color-contrast violation) | P10 axe rows |
| BUG-10 (see BUG-01) | P2 | one table now; 24 → ~100 airports; city codes explained | | | BUG-10-1, P4-R03, P6-S13, P6-ML-01/03/04 |

Not changed on purpose: conversational agent mode and plan-only mode do not exist and are not built
here (README "Pilot scope"); P7/P8 rows stay NOT_IMPLEMENTED with an accurate capability statement.
