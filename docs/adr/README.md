# Architecture Decision Records

Short, numbered, immutable once accepted (supersede with a new one). The frozen design package in
`docs/architecture/00-design-package.md` is the baseline; ADRs record where implementation refined it.

| # | Decision |
|---|---|
| [0001](0001-monorepo-with-gradle-convention-plugins.md) | Monorepo, Gradle 9 + Kotlin DSL, convention plugins, one version catalog |
| [0002](0002-temporal-and-kafka-have-different-jobs.md) | Temporal coordinates processes; Kafka broadcasts facts |
| [0003](0003-llm-never-authorizes.md) | The LLM proposes; deterministic policy authorizes; agents are machine identities |
| [0004](0004-offer-is-not-order.md) | Offers are ephemeral supplier promises; Orders are transactional truth |
| [0005](0005-idempotent-transaction-commands.md) | Every mutating command carries an idempotency key; uniqueness enforced in the owning table |
| [0006](0006-one-database-per-service-enforced-by-credentials.md) | One Postgres database and one login role per service |
| [0007](0007-money-in-minor-units.md) | Money is (ISO currency, integer minor units); no floating point, no implicit FX |
| [0008](0008-ids-are-prefixed-ulids.md) | Ids are `<prefix>_<ULID>`; tenant ids are slugs, not UUIDs |
| [0009](0009-llm-gateway-task-shaped-and-evidenced.md) | One LLM gateway; task-shaped calls; every call leaves evidence; narration degrades, transactions do not |
| [0010](0010-disruption-recovery-is-the-same-machine-with-a-second-trigger.md) | Disruption recovery reuses the planning machine: gateway-normalized notices, a Disruption aggregate with immutable decision/outcome records, a recovery workflow that asks policy `order.change`, one idempotent order change |
| [0011](0011-itineraries-are-components-with-one-transactional-truth.md) | Slice 3: itineraries are ordered components with stable ids; one supplier abstraction for air/hotel/ground with SIMULATED sandboxes; policy judges every offer and the whole; CP-SAT composes; Temporal keeps coordination state, Order keeps transactional truth; revalidation before the only mutation; exposure is a record; connected recovery |
| [0012](0012-demand-detection-is-deterministic-context-not-authority.md) | Slice 4: Enterprise Context owns identity and demand; deterministic, versioned rules with quoted evidence; one candidate per commitment with every source kept; explicit lifecycle; a person converts through Travel Core's CreateTrip (idempotent, arranger rule); sync is a Temporal workflow page by page with checkpoints at the write boundary; Slice 3 carry-overs |
| [0013](0013-learning-is-bounded-evaluated-and-reversible.md) | Slice 5: Learning owns an append-only outcome ledger and versioned profiles; `reliability-v1` is a smoothed, windowed, bounded soft preference (never money, never a hard rule); outcomes are recorded, never inferred, one row per logical revision; SANDBOX/LIVE classes never mix; chronological leak-free evaluation gates activation; OFF/SHADOW/ACTIVE with atomic, audited, reversible activation; planners pin inputs through one activity and fall back to the baseline |
| [0014](0014-profiles-organization-and-explicit-arranger-authority.md) | Phase 2: Enterprise Context owns traveler profiles (encrypted sensitive fields, redaction, logged disclosure, retention), guests, org units and projects (restricted to members), and explicit arranger grants; authority to arrange is a recorded relationship, never a request field or a bare MANAGER role; Travel Core asks over gRPC, books under the profile identity and snapshots the allocation; visibility and approvals follow the allocation; the client is optional and legacy trips keep Slice 1 rules |
| [0015](0015-planning-is-not-purchasing.md) | Phase 3: a trip is QUOTED between planning and commitment; a purchase authorization (HUMAN confirmation or policy-granted POLICY_AUTONOMY) bound to plan, price, currency, conditions, traveler and expiry gates the move to BOOKING in Travel Core and is consumed exactly once; re-quotes and re-selections supersede it; drafts, search preferences, booking-horizon policy rules and persisted conversations use the same trip APIs |
| [0016](0016-supplier-integrations-are-ledgered-capability-described-and-credential-gated.md) | Phase 4: the gateway ledgers every mutation before calling a supplier and answers retries from the ledger; a lost answer is reconciled by lookup or reported as OUTCOME_UNKNOWN (an UNKNOWN order item with an exposure, never a machine retry); capabilities state idempotency, reconciliation and negotiated-rate support; Duffel (air) and Hotelbeds (hotel) adapters exist only when credentials do and never book in tests; passengers are read from Enterprise Context at booking time under the agent identity; rail and car are distinct kinds; locations carry coordinates |
| [0017](0017-money-is-recorded-before-it-moves.md) | Phase 5: the Order service's finance ledger is authoritative; tokenized instruments (card numbers refused); authorize before the first supplier call, capture what suppliers charged, void or refund with idempotent events and provider references; FX provenance recorded, never computed; sandbox and credential-gated Stripe providers; supplier payables by settlement method; travel credits as value, not money; receipts and provider reconciliation; Learning's refund endpoint is a statement and consumes the ledger's events |
| [0018](0018-servicing-is-component-scoped-and-unfinished-business-is-a-case.md) | Phase 6: partial cancellation is a component-scoped release (Order `component_ids`, per-item refunds/credits, refusals as exposures, the order and trip keep their status); a traveler's request to move a flight is a `TRAVELER_REQUEST` disruption run by the same recovery with the person as the policy actor and the requested window as the constraint; unfinished business is a case in the assistance service (kinds, queues, SLAs, owners, next actions, escalation sweep, settled by the platform's own events) that never changes an order, a trip or a payment |
| [0019](0019-governance-is-scoped-budgeted-chained-and-federated.md) | Phase 7: scoped policies resolve most specific first; approval chains and step expiry come from the document; delegation is a recorded relationship; unanswered approvals escalate then expire; budgets are reserved under a lock before purchase and settled by the trip's own events; supplier agreements steer the search and the verdict; federation brokers the customer's IdP through Keycloak and provisioning is SCIM 2.0; genuine HRIS/calendar/CRM/expense adapters exist only when their credentials do |
| [0020](0020-people-hear-from-the-platform-through-durable-notifications.md) | Phase 8: a notification is a row before it is a message (one per fact and recipient, per-channel deliveries retried by a dispatcher, in-app always, email/chat credential-gated, preferences per category); recipients are people or roles; traveler safety is derived from booked trips (advisories, affected travelers, check-ins, cases for the silent); the itinerary exports as iCalendar and JSON from Travel Core |
| [0021](0021-reports-are-sums-of-what-the-events-said.md) | Phase 9: the audit service computes spend, outcomes, exceptions and supplier reports (JSON or CSV) from a derived per-trip fact row and the stored events; the allocation travels on trip events; nothing is converted or estimated |
